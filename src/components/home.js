export function home() {
    return /* html */ `
    <section id="home-section">
    <div id="background-pattern">
        <h1 id="intro"><span>Hi, I'm Ollie</span><br>Developer with a passion for<br>
        building, learning, and creating<br>
        unique digital experiences.</h1>
        <div id="find-out-more-div">
            <h2 id="find-out-more">FIND OUT MORE</h2>
            <div id="img-div">
                <img src="src/assets/arrow-down.svg">
            </div>
        </div>
    </div>
    </section>`
}

// let viewport = `${window.innerHeight}, ${window.innerWidth}`
// <p id="viewport">${viewport}</p>
// setInterval(()=>{document.getElementById('viewport').innerHTML = `${window.innerHeight}, ${window.innerWidth}`}, 100)
